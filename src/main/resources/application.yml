spring:
  application:
    name: ai-code
server:
  port: 8222
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.ace.aicode.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
